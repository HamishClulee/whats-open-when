{
  "license": "ISC",
  "scripts": {
    "start": "npm-run-all -p server web",
    "server": "cd $PWD/server && yarn start",
    "web": "cd $PWD/web && yarn serve"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
